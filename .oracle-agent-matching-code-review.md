You already reviewed this design (`agent-matching-signed-profile`) and then a plan (`agent-matching-plan-review-2`). The second review withheld a verdict because the spec/plan Markdown never reached the ChatGPT tab. This follow-up attaches those docs **and the shipped implementation**.

This is NOT a request to execute Task 1–9. The 9-task plan has already been implemented and committed on branch `codex/agent-id-matching` (commits `6280a79` … `0c53f0a`). Review **shipped code vs locked spec**. Do not ask us to start a new 9-task rewrite.

## Locked product decisions (do not reopen unless a concrete poisoning path remains)
- Deterministic unique-hit matching stays first. Agent only leftovers.
- Catalog/meta / `findExternalId()` never call a model.
- Runtime: `@earendil-works/pi-agent-core` + `@earendil-works/pi-ai`. Never `pi-coding-agent`.
- Tools: `get_douban_subject`, `search_tmdb`, `find_tmdb_by_imdb`, `get_tmdb_external_ids`, `lift_imdb_series`, `conclude_match`.
- No covers/vision, no Trakt **search** tool. Code may still fill Trakt from TMDB external IDs.
- Confidence **tiers** choose write destination; code verifier **gates** safety.
- Thresholds: `>=0.9` auto-write if verifier passes; `0.6–0.9` suggest; `<0.6` / `none` → `no_match` + backoff.
- `calibrated` is human-only. Agent never writes `true`.
- Enqueue only after cron’s own deterministic pass; hourly random cap `N=20`.
- `reason` stored truncated to 400 chars, overwrite per row.

## Explicit user rejection of your earlier IMDb-chain auto-write gate
In `agent-matching-signed-profile` you recommended: auto-write only when there is a Douban↔IMDb identity chain; items without an IMDb anchor go to human review.

The user rejected this. Reason: Douban IMDb is not fully trustworthy; if a usable IMDb exists, deterministic matching usually already finds TMDB, so those items never reach the agent. Auto-write is **confidence + verifier**, not an IMDb chain.

Locked compromise already in the spec:
- No Douban IMDb required for auto-write.
- If both Douban and candidate have IMDb, they must match; conflict **downgrades to suggest**.
- Missing year does **not** veto auto-write; obvious remake year conflict does.

Do **not** re-impose the IMDb-chain gate unless you can show a concrete residual poisoning path that confidence+verifier cannot catch, with an example from this codebase.

## What is attached
Must-read:
- Spec: `docs/superpowers/specs/2026-09-17-agent-id-matching-design.md`
- Plan: `docs/superpowers/plans/2026-09-17-agent-id-matching.md` (checkboxes still say unimplemented; ignore that — code is shipped)

Shipped implementation:
- `apps/core/src/cron.ts`
- `apps/core/src/libs/api/index.ts`
- `apps/core/src/db/schema.ts`
- `apps/core/src/libs/agent-match/{constants,types,candidates,verifier,tools,writer,pool,prompt,runner,queue}.ts`
- `apps/core/src/routes/dash/tidy-up/{index,detail}.tsx`
- `apps/core/src/index.tsx`
- `apps/core/wrangler.jsonc`
- Tests: `apps/core/test/agent-match-*.test.ts`, `cron.test.ts`, `tidy-up-agent.test.ts`, `persist-id-mapping.test.ts`

## Already absorbed from previous reviews (do not re-flag as missing)
- Dedicated conditional writer (`writer.ts`) with revision + token + `tmdb_id IS NULL` + uncalibrated + `agent_state=running`
- Cron awaits `persistIdMapping` before claiming (no `waitUntil` for persist)
- Cron no longer constructs `calibrated: true`
- Movie year unique-hit requires both years present
- IMDb parent lookup has local catch and continues title fallback
- Generic upsert keeps existing non-null TMDB (`CASE WHEN tmdb_id IS NULL THEN excluded ELSE current`)
- Episode results not registered as show IDs in `find_tmdb_by_imdb`
- Tidy-up confirm/reject/save bump revision and clear token/state/lease
- Empty form fields → null, not 0
- Queue consumer: ALS + ack after D1, `retry()` on throw
- Pi packages in `apps/core/package.json`

## Known spec mismatches we want you to confirm or refute
Please inspect the actual files, not this list. Highest priority first:

1. **Pool never re-queues evaluated `no_match`.** `pool.ts` `eligibleWhere` requires `agent_state='no_match' AND next_agent_at <= now AND agent_input_hash IS NULL`. Claim always **writes** a hash, so backoff is dead. Spec: skip only if hash is unchanged **and** last result was `no_match`/`suggested`; `no_match` may re-enter after `next_agent_at`. Suggested stays out until human review.

2. **Hash is garbage.** On first claim, `hashAgentInput({ doubanId, title: String(doubanId), type: "unknown", imdbId })`. Spec wants Douban title/year/type + policy version. Also means “same input skip” cannot work.

3. **`rejectedCandidateIds` stored on reject, never checked** by verifier/runner. Spec: hide rejected suggestions.

4. **Verifier weaker than spec §5.** Closed-set + type + IMDb conflict + movie year Δ≥3 → none. No title check. Spec says titles/years are veto/旁证; remake year conflict vetoes auto-write. IMDb conflict correctly downgrades to suggest.

5. **`get_tmdb_external_ids` does not fill Trakt.** Spec: if TMDB uniquely provides Trakt, **code** writes it. TMDB `external_ids` in this codebase only types `imdb_id`/`tvdb_id`.

6. **`lift_imdb_series` swallows errors as `{error:"imdb_lookup_failed"}`.** Spec: infra failure vs empty result; don’t pay the model / don’t write confident `no_match` on lookup_error.

7. **Cron still `waitUntil(queue.send)`.** Persist is awaited; enqueue is fire-and-forget. If send fails, claim stays `pending` until lease expiry. Spec: claim then send; expired pending recovered next hour.

8. **Queue claim → running does not check lease.** `runAgentMatchJob` updates to `running` on token+revision only.

9. **`persistIdMapping` still uses `doubanMappingSchema` with `z.coerce.number()`** on IDs. Tidy-up POST was fixed; generic persist path can still coerce `""`/`undefined` oddly if fed bad data.

10. **Plan file still describes Task 1–9 as unimplemented checkboxes**, but the code is already committed. Judge shipped code, not “ship Task 1”.

## Return exactly
1. Verdict: approve / approve-with-changes / reject. One sentence why. Judge **shipped code vs spec**, not the product idea.

2. The 3–5 remaining issues that would actually poison TMDB IDs, drop jobs, double-pay the model, or make hourly matching one-shot forever. Be concrete (file, SQL, verifier rule, queue ack). Skip ceremonial nits.

3. Spec vs code mismatches that are real. Confirm or refute the 10 items above. If an item is already safe, say so.

4. Tightest remaining auto-write risk given the user’s locked confidence policy. If you still want a tighter rule, propose the **smallest additive verifier check that does not require a Douban IMDb chain**. Prefer title/year/type/closed-set. Wrong posters are worse than missed matches.

5. Do **not** propose rewriting the deterministic matcher unless a specific remaining bug forces it. Prefer the smallest post-implementation delta.

If the attached Markdown/code is missing from this ChatGPT turn, say so immediately and do not invent file contents.
