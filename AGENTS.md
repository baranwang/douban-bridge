# PROJECT KNOWLEDGE BASE

**Generated:** 2026-05-23
**Commit:** d8c9af9
**Branch:** main

## OVERVIEW

Stremio addon for Douban catalogs, metadata, images, and ID mapping. Cloudflare Workers app using Hono, React SSR/hydration, Tailwind v4/shadcn UI, Drizzle over D1, KV/caches.default for caching.

## STRUCTURE

```text
stremio-addon-douban/
+-- src/index.tsx          # Worker fetch + cron export; route wiring
+-- src/cron.ts            # hourly ID-mapping backfill job
+-- src/client/            # browser-only hydration entries
+-- src/components/        # React UI; see child AGENTS.md
+-- src/db/                # Drizzle D1 schema/client
+-- src/libs/              # config, routing, sessions, middleware, API clients
+-- src/routes/            # Hono endpoints + SSR pages; see child AGENTS.md
+-- drizzle/               # generated SQL migrations and snapshots
+-- public/                # Worker static assets
+-- wrangler.jsonc         # Cloudflare bindings/routes/cron
`-- vite.config.ts         # Cloudflare + SSR components + Tailwind build
```

## WHERE TO LOOK

| Task | Location | Notes |
|------|----------|-------|
| Worker entry/mounts | `src/index.tsx` | middleware order and route mounts live here |
| Cron mapping job | `src/cron.ts` | uses AsyncLocalStorage context outside requests |
| Stremio manifest/catalog/meta | `src/routes/manifest.ts`, `src/routes/catalog.ts`, `src/routes/meta.ts` | dual public and `/:config` routes |
| Configure page | `src/routes/configure.tsx`, `src/client/configure.tsx`, `src/components/configure/` | SSR plus client hydration |
| Admin dashboard | `src/routes/dash/` | protected SSR mini-app |
| API integrations | `src/libs/api/` | context-bound clients; see child AGENTS.md |
| Config encoding/storage | `src/libs/config.ts` | base64url brotli for public configs; D1 for user configs |
| Resource path parsing | `src/libs/router.ts` | shared Stremio `catalog/meta/stream/subtitles` matcher |
| Sessions/auth | `src/libs/session.ts`, `src/routes/auth.ts` | JWT cookie plus GitHub OAuth |
| D1 schema | `src/db/schema.ts` | no foreign keys; app-level relationships |
| Migrations | `drizzle/*.sql` | generated by Drizzle; apply with Wrangler D1 command |
| Cloudflare bindings | `wrangler.jsonc`, `worker-configuration.d.ts` | regenerate types with `pnpm cf-typegen` |

## CODE MAP

| Symbol | Type | Location | Role |
|--------|------|----------|------|
| `app` | Hono app | `src/index.tsx` | global middleware and route composition |
| `scheduled` | Worker handler | `src/cron.ts` | hourly unmapped Douban ID calibration |
| `api` | singleton facade | `src/libs/api/index.ts` | provider clients plus ID mapping persistence |
| `BaseAPI` | class | `src/libs/api/base.ts` | axios fetch adapter, cache, dedupe, context DB/KV access |
| `configSchema` | Zod schema | `src/libs/config.ts` | persisted and encoded config contract |
| `matchResourceRoute` | route helper | `src/libs/router.ts` | parses Stremio resource URLs |
| `getDrizzle` | DB helper | `src/db/index.ts` | D1-bound Drizzle client |
| `doubanMapping` | table | `src/db/schema.ts` | Douban to TMDB/IMDb/Trakt mapping cache |

## CONVENTIONS

- TypeScript strict mode; `@/*` resolves to `src/*`.
- Biome formats/lints: 2 spaces, double quotes, 120 columns, import organization, Tailwind class sorting as info.
- Tailwind v4 config lives in `src/style.css`; no separate Tailwind config file.
- shadcn config: `new-york`, neutral base, CSS variables, Lucide icons, `cn()` from `@/libs/utils`.
- Cloudflare Workers runtime uses `nodejs_compat`; D1 binding `STREMIO_ADDON_DOUBAN`, KV binding `KV`.
- Drizzle schema source is `src/db/schema.ts`; migrations output to `drizzle/`.
- Package workflow is minimal: no test/lint/typecheck scripts; use `pnpm build` for full build validation.
- `dist/`, `.wrangler/`, `worker-configuration.d.ts`, and `drizzle/meta/` are generated outputs. Do not hand-author guidance from them.

## ANTI-PATTERNS (THIS PROJECT)

- Do not add Drizzle foreign keys via `.references()`. SQLite/D1 table rebuilds make FK cascades risky; enforce relationships in app code.
- Do not assume `process.env` works in deployed Workers. Prefer Cloudflare bindings from request context.
- Do not call context-bound API clients outside request/scheduled AsyncLocalStorage.
- Do not treat checked-in `dist/` as source of truth.
- Do not add tests unless a runner/config is introduced first; this repo currently has no test setup.
- Do not overwrite generated migration snapshots manually.

## UNIQUE STYLES

- Stremio endpoints are mounted twice: public (`/catalog`, `/meta`, etc.) and config-scoped (`/:config/catalog`, `/:config/meta`).
- Configure UI is SSR-rendered and separately hydrated by `src/client/configure.tsx`.
- Forward compatibility fields are emitted unconditionally; do not reintroduce User-Agent-specific response shapes.
- API calls are best-effort: enrichment failures usually degrade to `null`/`[]` rather than failing the Stremio response.
- Config URLs are brotli-compressed base64url JSON unless the path segment is a user UUID.

## COMMANDS

```bash
pnpm install
pnpm dev
pnpm build
pnpm deploy
pnpm cf-typegen
pnpm drizzle-kit generate
npx wrangler d1 migrations apply stremio-addon-douban --remote
```

## NOTES

- `package.json` deploy script uses `npm run build && wrangler deploy` even though the project uses pnpm.
- No `.github/workflows` CI exists.
- `wrangler.jsonc` contains custom domain, D1/KV bindings, rate limits, hourly cron, and observability.
- D1 migrations are not part of the deploy script; apply them explicitly.
- `a11y` linting is off in Biome; do not infer accessibility coverage from lint success.
