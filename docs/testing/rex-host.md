# Rex 宿主验证

`tools/rex-probe` 已删除。Widget 按 `@rexnow/libs` / `@rexnow/rslib-plugin` 实现（`Widget.http` 用 `statusCode`，`Widget.storage` 为 async）。

Rex 真机矩阵（列表 / 详情 / 密钥 / 失败回退）**未执行**。Node 里的 Widget 测试不能代替 Rex 请求计数或截图。不要编造设备结果。
